<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Post</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
    }

    h1 {
      color: #333;
      margin-bottom: 2rem;
    }

    form {
      background-color: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 50%;
      max-width: 300px;
    }

    input {
      width: 96%;
      padding: 0.8rem;
      margin: 0.5rem 0;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 1rem;
    }

    button {
      background-color: #007bff;
      color: white;
      padding: 0.8rem 1.5rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      width: 100%;
      margin-top: 1rem;
    }

    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>

<body>
  <h1>Edit Post</h1>
  <form action="/listings/<%= listing._id %>?_method=put" method="post">
    <input type="text" placeholder="Enter Title" name="listing[title]" value="<%=listing.title%>"><br><br>
    <input type="text" name="listing[image][url]" placeholder="enter url of image" value="<%= listing.image.url%>"><br><br>
    <input type="text" name="listing[description]" placeholder="enter description"value="<%=listing.description%>"><br><br>
    <input type="number" name="listing[price]" placeholder="price" value="<%= listing.price%>"><br><br>
    <input type="text" name="listing[country]" placeholder="country name" value="<%= listing.country%>"><br><br>
    <input type="text" name="listing[location]" placeholder="location" value="<%= listing.location%>"><br><br>
    <button>update</button>
  </form>
</body>

</html>